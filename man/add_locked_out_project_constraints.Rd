% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_locked_out_project_constraints.R
\name{add_locked_out_project_constraints}
\alias{add_locked_out_project_constraints}
\alias{add_locked_out_project_constraints,ProjectProblem,numeric-method}
\alias{add_locked_out_project_constraints,ProjectProblem,logical-method}
\alias{add_locked_out_project_constraints,ProjectProblem,character-method}
\title{Add locked out project constraints}
\usage{
add_locked_out_project_constraints(x, locked_out)

\S4method{add_locked_out_project_constraints}{ProjectProblem,numeric}(x, locked_out)

\S4method{add_locked_out_project_constraints}{ProjectProblem,logical}(x, locked_out)

\S4method{add_locked_out_project_constraints}{ProjectProblem,character}(x, locked_out)
}
\arguments{
\item{x}{\code{\link[=problem]{problem()}} object.}

\item{locked_out}{Object that determines which planning units that should be
locked out. See the Details section for more information.}
}
\description{
Add constraints to a project prioritization problem to ensure
that particular projects are not selected for funding by the solution.
For example, it may be desirable to lock out specific projects to examine
their importance to the optimal funding scheme.
}
\examples{
\dontshow{if (oppr::run_example()) withAutoprint(\{ # examplesIf}
# load data
data(sim_projects, sim_features, sim_actions)

# add column to the project data to indicate which projects should be
# locked. in particular, this column will lock the first project
sim_projects$locked_out <- c(TRUE, rep(FALSE, nrow(sim_projects) - 1))

# print project data
print(sim_projects)

# build problem with maximum weighted sum objective and $150 budget
p1 <-
  problem(
    sim_projects, sim_actions, sim_features,
    "name", "success", "name", "cost", "name"
  ) \%>\%
  add_max_wtd_sum_objective(budget = 150) \%>\%
  add_binary_decisions()

# print problem
print(p1)

# build another problem, and lock out the second project using numeric inputs
p2 <- p1 \%>\% add_locked_out_project_constraints(c(2))

# print problem
print(p2)

# build another problem, and lock out the projects using logical inputs
# (i.e., TRUE/FALSE values) from the sim_projects table
p3 <- p1 \%>\% add_locked_out_project_constraints(sim_projects$locked_out)

# print problem
print(p3)

# build another problem, and lock out the projects using the column name
# "locked_out" in the sim_projects table
p4 <- p1 \%>\% add_locked_out_project_constraints("locked_out")

# print problem
print(p4)

# solve problems
s1 <- solve(p1)
s2 <- solve(p2)
s3 <- solve(p3)
s4 <- solve(p4)

# print the projects selected for funding in each of the solutions
print(s1[, sim_projects$name])
print(s2[, sim_projects$name])
print(s3[, sim_projects$name])
print(s4[, sim_projects$name])
\dontshow{\}) # examplesIf}
}
\seealso{
Other constraints:
\code{\link[=add_locked_in_action_constraints]{add_locked_in_action_constraints()}},
\code{\link[=add_locked_in_project_constraints]{add_locked_in_project_constraints()}},
\code{\link[=add_locked_out_action_constraints]{add_locked_out_action_constraints()}},
\code{\link[=add_manual_locked_action_constraints]{add_manual_locked_action_constraints()}},
\code{\link[=add_manual_locked_project_constraints]{add_manual_locked_project_constraints()}}
}
\concept{constraints}
