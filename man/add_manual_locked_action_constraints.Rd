% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_manual_locked_action_constraints.R
\name{add_manual_locked_action_constraints}
\alias{add_manual_locked_action_constraints}
\alias{add_manual_locked_action_constraints,ProjectProblem,data.frame-method}
\alias{add_manual_locked_action_constraints,ProjectProblem,tbl_df-method}
\title{Add manually specified locked constraints for actions}
\usage{
add_manual_locked_action_constraints(x, locked)

\S4method{add_manual_locked_action_constraints}{ProjectProblem,data.frame}(x, locked)

\S4method{add_manual_locked_action_constraints}{ProjectProblem,tbl_df}(x, locked)
}
\arguments{
\item{x}{\code{\link[=problem]{problem()}} object.}

\item{locked}{\code{data.frame} or \code{\link[tibble:tibble]{tibble::tibble()}} object. See
the Details section for more information.}
}
\value{
A \code{\link[=problem]{problem()}} object with the constraints added to it.
}
\description{
Add constraints to a project prioritization problem to ensure
that particular actions are selected, or not selected, for funding
by the solution. This function offers
more fine-grained control than the \code{\link[=add_locked_in_action_constraints]{add_locked_in_action_constraints()}}
and \code{\link[=add_locked_out_action_constraints]{add_locked_out_action_constraints()}} functions.
}
\details{
The argument to \code{locked} must contain the following columns.

\describe{

\item{\code{"action"}}{\code{character} values with action names.}

\item{\code{"status"}}{
\code{numeric} values indicating if actions should
be selected for funding (with a value of 1) or not (with a value of zero).
}

}
}
\examples{
\dontshow{if (oppr::run_example()) withAutoprint(\{ # examplesIf}
# load data
data(sim_projects, sim_features, sim_actions)

# create data frame with locked statuses
locked_data <- data.frame(
  action = sim_actions$name[1:2],
  status = c(0, 1)
)

# print locked statuses
print(locked_data)

# build problem with minimum set objective and targets that require each
# feature to have a 30\% chance of persisting into the future
p <-
  problem(
    sim_projects, sim_actions, sim_features,
    "name", "success", "name", "cost", "name"
  ) \%>\%
  add_max_wtd_sum_objective(budget = 500) \%>\%
  add_manual_locked_action_constraints(locked_data) \%>\%
  add_binary_decisions()

# print problem
print(p)

# solve problem
s <- solve(p)

# print solution
print(s)
\dontshow{\}) # examplesIf}
}
\seealso{
Other constraints:
\code{\link[=add_locked_in_action_constraints]{add_locked_in_action_constraints()}},
\code{\link[=add_locked_in_project_constraints]{add_locked_in_project_constraints()}},
\code{\link[=add_locked_out_action_constraints]{add_locked_out_action_constraints()}},
\code{\link[=add_locked_out_project_constraints]{add_locked_out_project_constraints()}},
\code{\link[=add_manual_locked_project_constraints]{add_manual_locked_project_constraints()}}
}
\concept{constraints}
