% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_locked_in_action_constraints.R
\name{add_locked_in_action_constraints}
\alias{add_locked_in_action_constraints}
\alias{add_locked_in_action_constraints,ProjectProblem,numeric-method}
\alias{add_locked_in_action_constraints,ProjectProblem,logical-method}
\alias{add_locked_in_action_constraints,ProjectProblem,character-method}
\title{Add locked in action constraints}
\usage{
add_locked_in_action_constraints(x, locked_in)

\S4method{add_locked_in_action_constraints}{ProjectProblem,numeric}(x, locked_in)

\S4method{add_locked_in_action_constraints}{ProjectProblem,logical}(x, locked_in)

\S4method{add_locked_in_action_constraints}{ProjectProblem,character}(x, locked_in)
}
\arguments{
\item{x}{\code{\link[=problem]{problem()}} object.}

\item{locked_in}{Object that determines which planning units that should be
locked in. See the Details section for more information.}
}
\value{
A \code{\link[=problem]{problem()}} object with the constraints added to it.
}
\description{
Add constraints to a project prioritization problem to ensure
that particular actions are selected for funding by the solution. For
example, it may be desirable to lock in actions for conserving culturally or
taxonomically important species.
}
\details{
The locked actions can be specified in several different ways:

\describe{

\item{\code{integer} vector}{
Each values specifies the index for an action that should be locked when
generating solutions
(i.e., row numbers of the actions in the argument to \code{actions} in
\code{\link[=problem]{problem()}}).
}

\item{\code{logical} vector}{
Each value (i.e., \code{TRUE} and/or \code{FALSE}) indicates
if an action should be locked (or not) when generating the solution.
These \code{logical} values should correspond to each row in the argument to
\code{actions} in \code{\link[=problem]{problem()}}.
}

\item{\code{character} value}{
The value specifies the name of a column in the action data
(i.e., argument to \code{actions} in \code{\link[=problem]{problem()}}). The column must
have \code{logical} (i.e., \code{TRUE} and/or \code{FALSE}) values, and these
values are used to indicate which actions are locked (or not).
}

}
}
\examples{
\dontshow{if (oppr::run_example()) withAutoprint(\{ # examplesIf}
# load data
data(sim_projects, sim_features, sim_actions)

# print action data
print(sim_actions)

# build problem with maximum weighted sum objective and $150 budget
p1 <-
  problem(
    sim_projects, sim_actions, sim_features,
    "name", "success", "name", "cost", "name"
  ) \%>\%
  add_max_wtd_sum_objective(budget = 150) \%>\%
  add_binary_decisions()

# print problem
print(p1)

# build another problem, and lock in the 3rd action using numeric inputs
p2 <- p1 \%>\% add_locked_in_action_constraints(c(3))

# print problem
print(p2)

# build another problem, and lock in the actions using logical inputs from
# the sim_actions table
p3 <- p1 \%>\% add_locked_in_action_constraints(sim_actions$locked_in)

# print problem
print(p3)

# build another problem, and lock in the actions using the column name
# "locked_in" in the sim_actions table
p4 <- p1 \%>\% add_locked_in_action_constraints("locked_in")

# print problem
print(p4)

# solve problems
s1 <- solve(p1)
s2 <- solve(p2)
s3 <- solve(p3)
s4 <- solve(p4)

# print the actions selected for funding in each of the solutions
print(s1[, sim_actions$name])
print(s2[, sim_actions$name])
print(s3[, sim_actions$name])
print(s4[, sim_actions$name])
\dontshow{\}) # examplesIf}
}
\seealso{
See \link{constraints} for an overview of functions for adding constraints.

Other constraints:
\code{\link[=add_locked_in_project_constraints]{add_locked_in_project_constraints()}},
\code{\link[=add_locked_out_action_constraints]{add_locked_out_action_constraints()}},
\code{\link[=add_locked_out_project_constraints]{add_locked_out_project_constraints()}},
\code{\link[=add_manual_locked_action_constraints]{add_manual_locked_action_constraints()}},
\code{\link[=add_manual_locked_project_constraints]{add_manual_locked_project_constraints()}}
}
\concept{constraints}
