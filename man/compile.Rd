% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compile.R, R/multi_compile.R
\name{compile}
\alias{compile}
\alias{compile.ProjectProblem}
\alias{multi_compile.MultiObjProjectProblem}
\alias{multi_compile.list}
\title{Compile a problem}
\usage{
compile(x, ...)

\method{compile}{ProjectProblem}(x, n_approx = 100, ...)

\method{multi_compile}{MultiObjProjectProblem}(x, ...)

\method{multi_compile}{list}(x, ...)
}
\arguments{
\item{x}{\link[=ProjectProblem-class]{ProjectProblem} object.}

\item{...}{not used.}

\item{n_approx}{\code{integer} number of points to use for piece-wise
linear approximations of non-linear terms.
Defaults to 100.}
}
\value{
An \link[=OptimizationProblem-class]{OptimizationProblem} object.
}
\description{
Compile a project prioritization \code{\link[=problem]{problem()}} into a general
purpose format for optimization.
}
\details{
This function might be useful for those interested in understanding
how their project prioritization \code{\link[=problem]{problem()}} is expressed
as a mathematical problem. However, if the problem just needs to
be solved, then the \code{\link[=solve]{solve()}} function should be used instead.
}
\examples{
# load data
data(sim_projects, sim_features, sim_actions)

# build problem with maximum weighted sum objective, $200 budget, and
# binary decisions
p <-
  problem(
    sim_projects, sim_actions, sim_features,
    "name", "success", "name", "cost", "name"
  ) \%>\%
  add_max_wtd_sum_objective(budget = 200) \%>\%
  add_binary_decisions()

# print problem
print(p)

# compile problem
o <- compile(p)

# print compiled problem
print(o)
}
