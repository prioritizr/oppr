# Is waiver?

Check if an object is a `waiver`.

## Usage

``` r
is.Waiver(x)
```

## Arguments

- x:

  Object.

## Value

A `logical` value.

## Examples

``` r
# create new waiver object
w <- new_waiver()

# is it a waiver object?
is.Waiver(w)
#> [1] TRUE
```
