context("add_min_set_objective")

test_that("compile", {
  # create data

  # create problem

  # create optimization problem
  o <- compile(p)
  # tests
})

test_that("solve", {
  skip_on_cran()
  skip_if_not(any_solvers_installed())
  # create data

  # create problem

  # solve problem

  # tests

})
